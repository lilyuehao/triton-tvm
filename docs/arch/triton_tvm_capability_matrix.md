# Triton-TVM Capability Matrix

The active public alpha capability matrix lives at:

- `docs/arch/triton_tvm/capability_matrix.md`

It describes current support and non-support boundaries without relying on
internal planning reports.
